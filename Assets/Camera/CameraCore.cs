using UnityEngine;

/// <summary>
/// initializes everything the camera scripts need
/// </summary>

public class CameraCore : MonoBehaviour {

    // take bird's rigidbody (for camera follower)

    void Start() {
        
    }

    void Update() {
        
    }
}
